{
  "name": "trading-journal",
  "version": "1.0.0",
  "description": "A trading journal application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build process would go here'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t trading-journal .",
    "docker:run": "docker run -p 8080:80 trading-journal"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
} 